__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.ipynb_checkpoints/
.venv/
venv/

# 受数据使用权限制的序列，既不进仓库也不进构建产物。
# 构建侧的排除在 pyproject.toml 与 MANIFEST.in，三处共同保证它不外流。
src/alpholio/data/

# 运行产物与缓存（均可由代码重新生成）
outputs/
logs/
cache/
site/

# 数据文件不进仓库。文档中的示例只引用输入表的前几行，不附带数据本身。
*.feather
*.parquet

# 本地编辑器与 AI 助手配置，不进仓库
.claude/
.vscode/

# 草稿 notebook
*.ipynb
